# Benchmark result artifacts

Generated outputs under this directory are **gitignored**.
Reproduce with:

```bash
python benchmark/run_benchmark.py --mode tiny --seed 42 --out-dir benchmark/results/tiny
mobiroute demo --out-dir benchmark/results/demo
```

Claim level for default runs: `synthetic_benchmark` only.
